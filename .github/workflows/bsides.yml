name: BSides Montreal 2023

on: workflow_dispatch

jobs:
  hello-world: 
    runs-on: ubuntu-latest
    steps:
      - run: |
            id
            uname -a
            env
            echo "Hello Bsides Montreal"
